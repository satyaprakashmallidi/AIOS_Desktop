node_modules/
dist/
release/
build/dist/
build/build/

# Logs / temp
*.log
*.tmp
.DS_Store

# Env (don't commit real secrets)
.env
*/.env
!.env.example
!**/.env.example

# Local databases / archives
*.db
*.sqlite
*.zip
__MACOSX/

# Tooling caches / dev artifacts
.playwright-mcp/
.edge-profile/
.edge-profile2/
.edge-profile3/
.edge-profile4/
.aios-dev-user-data/
supabase/.temp/
supabase/supabase/.temp/
__pycache__/
aios_app/
.venv/
venv/

# Local Claude Code config (contains shell command history with secrets)
.claude/settings.local.json

# Third-party clones kept locally for cross-referencing while we
# extend Computer Control. Not part of AIOS — never committed.
reference/

# Junk screenshots in root
aios-chat-check.png
chat-ui-check.png
chat-ui-check-fixed.png
.claude/worktrees/
