# System: [Name]

> One-line description of what this system does.

## Architecture

Brief architecture overview. Use an ASCII diagram if it helps:

```
[Input] --> [Processing] --> [Output]
```

## Key Files

| File | Purpose |
|------|---------|
| `path/to/main-file` | What it does |
| `path/to/config` | Configuration |
| `path/to/helper` | Helper functions |

## How It Works

Step-by-step flow of the system's main operation:

1. First this happens...
2. Then this processes...
3. Finally this outputs...

## Configuration

Environment variables, config files, or credentials needed:

| Variable | Purpose | Required |
|----------|---------|----------|
| `EXAMPLE_KEY` | What it's for | Yes |

## Common Operations

**Start the system:**
```bash
python3 scripts/example.py
```

**Debug issues:**
- Check the logs at...
- Common error: ... → Fix: ...

## Dependencies

- **Depends on:** What this system needs to run
- **Used by:** What other systems use this one

## History

| Date | Change |
|------|--------|
| YYYY-MM-DD | Initial documentation |
